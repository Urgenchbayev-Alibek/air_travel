import 'package:flutter/material.dart';

class ViloyatlarDropdownWidget extends StatelessWidget {
  const ViloyatlarDropdownWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
